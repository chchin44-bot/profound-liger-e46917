# v93 這一輪改了什麼（給審查用）

## ⚠️ 工程契約（v93d 起改變，請先讀這段）

**`index.html` 是唯一主檔，日常修改直接改它。**

這個專案本來就是 GitHub Pages，實際部署的也是 `index.html`，所以流程維持最直接：

```
改 index.html → 跑測試 → ./sync-source.sh → commit → push
```

`source.html` 與 `build.sh` 的定位改成**工程輔助／重建工具**，不再是修改的必經之路。
之前兩份都被當成 source of truth，最大的風險是有人拿比較舊的 `source.html` 重新 build，
把已經修好的 `index.html` 蓋掉——第 6 次容器回捲之後就差點發生。

現在有兩道防護：

- `build.sh` 預設**拒絕執行**（它會用 `source.html` 覆蓋 `index.html`）。
  真的要從底稿重建才下 `REBUILD_FROM_SOURCE=1 ./build.sh`；而且它會先比對兩者的時間，
  `source.html` 比 `index.html` 舊就直接擋下。
- `sync-source.sh` 反過來把 `index.html` 反推成 `source.html`，讓底稿不會漂移。

---

> 建置標記 `v92`，`VERSION` 檔停在 92 —— 版本號是建置時填入的，這幾輪都在 92 底下迭代。

---

## 你應該看哪幾個檔案

| 檔案 | 是什麼 | 為什麼要看 |
|---|---|---|
| **`index.html`** | 產品。單一 HTML，離線可開 | 這是唯一真正要交付的東西 |
| **`source.html`** | 衍生底稿（由 index 反推） | 好讀、好 diff；**不是**修改入口 |
| `CHANGES-v93.md` | 這份文件 | 改動總覽與檔案指引 |
| `build.sh` | 建置腳本（含三道防線） | 底稿健檢／版本守門／自動備份 |
| `staleguard.sh` | 開工前的狀態檢查 | 容器回捲時第一時間叫停 |
| `run-suite.sh` / `tests.manifest` | 回歸套件與測試清單 | 見文末「目前狀態」 |

`index.html`（或 `source.html`）裡搜以下關鍵字可以直接跳到這輪的改動：
`ANIMAL_META`、`TACTIC_META`、`PE_BASIS_COPY`、`DEFAULT_FS`、`LAYER_NOTE`、`peCopy`、`animalFacet`、`chartbox`。

---

## 一、產品架構：四層

說明頁新增「這一頁分成四層」，並且每一層都標明下一層在哪裡。

| 層 | 回答什麼 | 在哪裡 |
|---|---|---|
| 第一層 | 發生什麼事？ | 動物標籤 🐂🐻🐋🐊…**只描述狀態，不是買賣指令** |
| 第二層 | 現在價格在哪？ | 便宜／合理／偏貴參考價、季線、布林區間 |
| 第三層 | 照這套規則可以怎麼做？ | 調倉區間與出場統計，**會給具體數字與動作** |
| 第四層 | 這些建議有什麼限制？ | 每個門檻旁的「證據程度」與「⚖️ 門檻與證據」分頁 |

之前的錯不是「有建議」，而是把第一層當第三層用（🐻 出現＝該買）。修法不是把建議整頁拿掉，
而是分層並互相指路。說明頁也加了一段「為什麼不乾脆一句建議都不給」——因為這一頁確實算得出
508／600／708，假裝沒有只會讓使用者自己腦補一套更差的。

---

## 二、動物法則：收斂成單一 metadata

**改動位置：`index.html` 搜 `ANIMAL_META`**

以前同一隻動物的資料散在三個手抄副本（`ANIMALS[].d`、`ANIMAL_TRIGGER`、說明頁表格），
三份不同步的後果全中。現在一份 metadata，其餘全部衍生。

每隻動物有八個欄位：`ttl` 白話標題／`st` 發生什麼／`you` 跟你有什麼關係／`can` 可以怎麼做／
`tr` 觸發條件／`src` 資料來源／`ev` 證據程度／`lim` 它不知道什麼。

`can` 那一欄的契約（v93d 定版，前一版寫得太嚴、實作也對不上）：

- ✅ 檢查／確認／回想／寫下來
- ✅ **負向護欄**——「不要只憑這一個訊號買賣」「別因為大家都在漲就追高」
- ✅ 指路——具體價格在第二層、可以怎麼做在第三層
- ❌ **正向操作建議**——「可以考慮分批投入」「加碼」「減碼」，那一類留在第三層

理由：第一層回答「發生什麼事」。動物標籤如果自己就給得出正向操作，使用者會停在第一層
不再往下看——而第一層根本沒有價格。

### 修掉的三個實際與描述不符

| | 問題 | 處理 |
|---|---|---|
| 🐎 駿馬 | 三處都寫「還在趨勢中」，但程式只檢查 `pnl > 30 && level !== 'sell'`，**從來沒有驗過任何趨勢** | 文字只講程式真的驗過的兩件事；`lim` 直接寫「系統**沒有**檢查任何趨勢、均線或動能」 |
| 🦁 獅子 | 觸發條件寫「或在系統判定 🟢 便宜位階時你選擇不賣出」，**全檔搜尋不到那條路徑** | 刪掉那半句，不是補一個假實作 |
| 🐖 豬 | 個股列 100% 是估值觸發，說明卻寫「估值 **或** 集中度」，使用者得自己猜 | `stockAnimals()` 改回傳 `pig@val`／`pig@conc`，標籤直接寫「🐖 豬 · 估值過熱」「· 持股太集中」 |

🐋 **刻意沒有跟著加 provenance**：v90 已經用「拿掉一條觸發」修好它了（當時「便宜但大戶在賣」
的股票會同時掛 🐋 與 🦈，你自己回報過 9904、9917）。加回 why 等於把那個衝突放回去。
這個決定寫在 metadata 的註解裡，防止下一個人「順手」補上。

### 動物卡：白話在上、證據在下

點任何動物標籤，看到的是三段白話（發生什麼／跟你有什麼關係／可以做什麼），
證據收在可展開的「為什麼會出現這隻動物？」裡——展開才有門檻、資料日期、來源、限制，
而且**印的是這一檔實際量到的數字**（例如「這一檔現在的本益比 40.0 倍」），不是寫死的範例。

🐋／🦈 另外永遠帶一句「⚠ 這是觀察訊號，不要單獨拿它決定買賣」（它們沒有幅度門檻，
+0.01% 也算，隨機下約四分之一的股票會符合）。

### 心理快篩

三段結語原本引用了這份檔案自己驗證不了的實證主張（「恐慌性賣出是長期報酬的最大殺手」
「研究一再顯示」「這比選股能力更值錢」）。改成只講機制、不宣稱統計結論——
因為同一份檔案在動物門檻那邊已經誠實承認「本頁自己訂的、沒有回測」，不該有兩套證據標準。

---

## 三、價格層：白話化 ＋ 資料血緣

**改動位置：`index.html` 搜 `PE_BASIS_COPY`、`peCopy`、`PE_BASIS_SUB`**

三張卡從「🟢 本益比低檔換算價 / EPS × 12 倍」改成：

```
🟢 便宜參考價   $508.0 元以下
   以這家公司自己過去的估值來看，跌到這裡算進入相對便宜的一段。
   計算依據：EPS × 12.7 倍（這檔自己近 4.9 年本益比的低檔）
```

下面接**條件式建議**，位置由現價比出來：便宜區給「如果你本來就打算長期持有，
與其猜最低點可以考慮分批」；偏貴區給「已經持有就檢查部位比例，還沒買就別急著追」。

### 修掉的資料血緣錯誤

`peBands()` 有三條路（自身歷史 → 同業中位數 → 產業慣例），但三張卡原本**不論走哪一條
都寫死「這家公司自己過去」**。一檔歷史不足的股票，數字其實是拿同產業算的，
小白讀到的第一句卻說是它自己的歷史。文案現在跟著 `basis` 走，`ind`／`conv` 還會在卡片上方
先出現一段紅框「⚠ 下面三個價格不是用這家公司自己的歷史算的」。

另外多一組 `unknown`：`t.bands` 會被存進本機快取，可能讀到 v93 之前存的、沒有文案欄位的舊物件，
直接取用會丟 TypeError 讓整個面板變空白——比文案說錯還糟。

**統計翻譯更正**：合理價用的是 P50（中位數）不是眾數。原本寫「過去最常出現的估值位置」，
雙峰分布時中位數那一格反而可能很少出現。改成「過去約有一半的時間比它便宜，一半比它貴」。

**產品契約更正**：面板頂端原本寫「本頁不提供買賣建議」，但下面幾行就寫著
「如果你本來就打算長期持有，可以考慮分批」——兩句不能同時成立。改成
「提供**規則化的價格與操作參考**，但那**不是針對你個人財務狀況量身制定的投資建議**」。
不推介個股、不保證報酬這兩條界線一個字都沒放寬。

**`ind`/`peer` key 漂移**：面板底部的來源說明是第二份手抄的 basis 對照表，
而且比對的是 `'peer'` —— `peBands()` 從來沒回過這個 key（它回 `'ind'`）。
於是同業中位數那條路永遠掉進 `else`，畫面寫成「產業慣用區間」。
修法不是把 `'peer'` 改成 `'ind'`（那還是兩份），而是整段刪掉、改讀 `PE_BASIS_COPY[].foot`。

---

## 四、字級與版面

- **預設字級從 `big` 改成 `mid`（舒適）**，按鈕從「小／中／大」改成「標準／舒適／大字」。
  預設值只有一個決定點 `DEFAULT_FS`（之前 state 初始值與 `applyFontScale()` 的 fallback
  各自寫死 `'big'`）。
- **股價原本完全不參與字級縮放**（sm/mid/big 都是同一個數字），大字模式下比股票名稱還小、
  比按鈕還小。同類問題還有：賺賠％／千張大戶／負債比／本益比四個數據方塊、主要按鈕。全部補上縮放。
- **資料日期徽章**「⛔ 資料 110 天前」一般模式只有 9.5px —— 那是「這個價格可不可信」的唯一標示。抬到 12／13／15px。
- **`.pe-note` 溢出**：手機覆寫只解掉 `max-width` 忘了 `min-width`，320px 上整段往右溢出 35px
  被裁掉（而且一般字級溢得比大字模式更多）。改成 `min-width:min(170px, 100%)`。
- **觸控目標**：自選列的編輯／刪除欄是 42px（≤400px 時 38px），但同一份 CSS 另一處自己寫著
  「窄畫面一律維持 44px」。改成一律 44px。
- 「元以下／左右／元以上」改成跟價格同一行（baseline 對齊，窄畫面才換行）。

---

## 五、自我測試（`index.html` 內建，網址加 `#selftest`）

- `runSelfTest()` 的 state 還原做成完整交易：`restoreWatchlist()` 清空陣列內容後依備份重建，
  保留 array identity，也補得回被 `applySnapshot()` 篩掉的非 top100 項目。
  實證：舊版跑完自我測試後 101 筆變 100 筆；新版 101 → 101。
- 新增 TC-015 直接測這支函式。目前 **30 項全過**。
- I6／I8 的說明更正：I6 是帳本內部的現金守恆，但它取用被測程式自己記錄的欄位，
  「連記錄一起錯」它抓不到；**真正獨立的 oracle 是 I8**。

---

## 五之二、v93d 這一輪（第三層與 provenance）

### 策略卡不再自己猜資料來源
`tacticFor()` 的 A 卡與 F 卡寫死「這檔自己的便宜價」「自身歷史低檔區」，但 `peBands()`
有三條路。走同業或產業慣例 fallback 時，畫面就在說一件沒發生的事——跟先前 footer 的
`'peer'` 幽靈 key 是同一種漂移。現在兩張卡的標題與正文都讀 `PE_BASIS_COPY[].zone / .ref`。

實測（同一檔、只改有沒有足夠歷史）：

| basis | F 卡標題 |
|---|---|
| `self` | 本益比位於**自身歷史低檔區** |
| `conv` | 本益比位於**產業慣用區間的低檔（未經回測）** |

`PE_BASIS_SUB()` 也一樣：原本「非 self/ind 一律當成產業慣例」，`unknown`（舊快取）
會掉進去又猜錯一次。改成只有真的是 `conv` 才那樣說，其餘留白。

### 第三層說明頁收斂成單一來源：`TACTIC_META`
說明頁那張「系統給的動作」表以前是 `tacticFor()` 的第二份手抄副本，兩邊演化到互相矛盾：
說明頁還寫 D「停止加碼、提高現金比」、W「停止加碼」、G「照原計畫定期定額」，
而程式裡早就沒有那些祈使句。現在 icon／title／情境／一句話說明由 `TACTIC_META` 提供，`tacticFor()` 與說明頁都讀它。
（**例外**：V／X 這兩張是閘門狀態卡，會用當下的判定覆寫 icon 與 title，因為它們要說出
「為什麼被擋下」。完整契約見下面 v93e 那一節。）

順帶清掉三處漏網的祈使句：`valuate()` 的「停止加碼並觀察獲利是否跟上」、
季線 −5% 的「應停止加碼、提高現金比重」、布林上軌的「短線也該在此分批獲利了結」。

### 撤掉一句沒有回測支撐的統計宣稱
A 卡原本寫「歷史上出現這個組合時，X～Y 是價格曾經停留過的區間」——那是統計宣稱，
但程式看得到的只有現在兩條線的 min/max，從沒回測過任何歷史組合。
改成「這兩條公式**目前**算出來的參考區間」。

### 動物 `can` 的契約重新定義
原本寫「只能檢查／確認／回想」，太嚴，實作也對不上。新契約：

- ✅ 檢查／確認／回想／寫下來
- ✅ **負向護欄**——「不要只憑這一個訊號買賣」「別因為大家都在漲就追高」
- ✅ 指路——具體價格在第二層、可以怎麼做在第三層
- ❌ **正向操作建議**——「可以考慮分批投入」「加碼」「減碼」，那一類留在第三層

掃過 18 隻，只有 🐻 違規（「與其猜最低點，可以考慮分批」），已改成指路。

### 大盤走勢圖填滿容器（使用者回報「線太窄不好看」）
那一格是 4 欄網格的 `col-span-3`，預設 `align:stretch`，高度被右邊那疊統計方塊撐到
400～500px；但 canvas 寫死 `height="118"`，圖只畫在最上面約四分之一。
改成 `.chartbox` 容器撐滿 ＋ Chart.js `maintainAspectRatio:false`。
實測 1280px：canvas 由 118px → **361px**，跟右欄對齊；390px 手機為 210px，無橫向溢出。

---

## 五之三、v93e（收尾 ＋ priceLadder）

### `priceLadder` 定位為第二層，動作詞全部拔掉
它回答的是「現在的價格離哪一條線最近、差幾 %」——那是位置，不是決策。

| 原本 | 現在 |
|---|---|
| 短期超賣**買點** 🐆 | 短期跌過頭參考價 🐆 |
| 長期便宜價 🐂 | 長期便宜參考價 🐂 |
| 中期轉弱價 🦏 | 中期轉弱參考價 🦏 |
| 中期**加碼價** 🐋 | **最近三個月平均價** 🐋 |
| 長期合理價 📊 | 長期合理參考價 📊 |
| 短期超買**賣點** 🐱 | 短期漲過頭參考價 🐱 |
| 長期昂貴價 🐖 | 長期偏貴參考價 🐖 |

最該拔的是「中期加碼價」——那條線就是 60 日均線。**三個月平均價是事實，加碼價是決策**，
兩件事不能共用一個名字。

`buildWatchMoves()` 的外層標題一起改（否則只改一半：內容說「三個月平均價」、標題說「買點」）：
「⬇ 往下的觀察買點」→「⬇ 現價下方可以觀察的價位」，上方同理。

### 產生數字的 basis，就是解釋數字的 basis
A/F 卡的文案原本各自再呼叫一次 `peCopy(peBands(d.ind, d))`，等於「希望第二次算出同一個答案」。
但 `peBands()` 不是純函式——它的同業 fallback 會讀當下的 watchlist。
現在 `tacticFor()` 一開始就抓住真正產生 `t.cheap/fair/rich` 的那一次：

```js
const BND = t.bands || (V && V.bands) || peBands(d.ind, d);
const BCP = peCopy(BND);
```

往下全部沿用，`tac(k, BCP)` 也改成吃已經決定好的文案——**metadata 不該有能力自己取得資料**。
順帶把 C/R/W 卡裡重算 `peBands()` 的三處也換掉：現在 `tacticFor()` 內的**重算次數為 0**，
只保留最上面那一次 fallback 呼叫（`t.bands` 拿不到時才用）。

### `TACTIC_META` 的 contract 寫準
原本寫「icon／title 只有這一份」，但 V（估值失真閘門）與 X（示範值／過期）會用當下的判定
覆寫 icon 與 title，因為它們要說出「為什麼被擋下」。那是刻意的，不是漏網的副本——
契約改成：**TACTIC_META 是策略類型的固定 metadata；閘門狀態可以覆寫具體 title/icon。**

---

## 五之四、v93e-2：實證主張清查（empirical claim sweep）

### 產品哲學（這一輪定案）

> 絕大多數的實證主張改寫成**機制**（這件事為什麼會影響你），而不是宣稱一個沒有來源的統計結果。
> 只有同時滿足兩個條件的才保留引用：
> **①** 它是原始提出者對自己方法的定義或校正，不是第三方的實證排名；
> **②** 本頁的數字不依賴它成立——引用只是幫使用者理解，拿掉也不會讓算式失效。

理由：這一頁在第四層已經誠實寫著「本頁自己訂的、沒有回測」，但第一層的大字還在說
「歷史報酬較好」「最大殺手」。**小字誠實救不了大字過度宣稱。**
而少數真的有出處、又對使用者有價值的（±2σ 到底是不是 95%），把出處放出來比含糊帶過誠實。

程式裡新增 `SOURCES`（目前兩筆），第四層有一張表列出「引用了什麼、為什麼引用它」，
並且說明其餘為什麼一律改寫成機制。

### 改掉的（P2）

| 原文 | 改成 |
|---|---|
| 「報酬的最大殺手不是市場，是自己」 | 「這三題用來檢查幾種常見的情況：原本設好的規則，容易在什麼時候被當下的情緒取代」 |
| 「三種最傷報酬的行為偏誤」 | 「三種本頁會檢查的決策偏誤」 |
| 大盤 −10%「歷史統計說長期報酬較好」／+15%「後續報酬較差」 | 「**本頁沒有驗證過這個位置之後的報酬**」（🐻 的 `lim` 也有同一句，一起改） |
| 「依 4% rule 的原始定義每年隨物價調升 2%」 | **事實更正**：Bengen (1994) 是依**實際通膨**調整，不是固定 2%。固定 2% 是本頁的模型假設，為了讓情境可重現——畫面與程式註解都寫明差在哪 |
| 「報酬順序風險跟你選什麼股票無關」 | 「即使總報酬相近，壞的那幾年若集中在提領期一開始，提領會放大傷害」（拿掉「無關」這個絕對詞） |
| 「時間會把黑天鵝變成一次超額報酬的補貨機會」 | 「在這個**固定會復原**的模型假設下，持續投入會在低價買到更多單位——真實市場不保證復原，單一公司更可能是永久性損失」 |

### 改掉的（P3）

| 原文 | 改成 |
|---|---|
| 「存股族長期報酬的一大半」 | 「股息也是總報酬的一部分，而本頁沒有記錄它——所以帳面損益不是完整總報酬」 |
| 布林通道「實際涵蓋率約 88～90%」 | 採用作者自己的說法：**John Bollinger 官方規則寫明預設參數實務上通常約 90%，不是 95%**（三處都改，並附出處） |
| 🐋「大戶的錢多、資訊通常也比較多」 | 「這代表大額持股者的持股比例連續兩週增加；原因與後續方向本頁不知道」 |
| 🐋「隨機下每四檔就有一檔」 | 補上出生證明：「把每週增／減簡化成各半且互相獨立，隨機就有 25% 機會連兩週增加」 |
| 🦏「利率上升或訂單變少，通常是最先出事的那一批」 | 講機制：「借得多，代表利息與還款壓力對利率變化、現金流變化更敏感」 |
| 「實務上常見做法是準備 2～3 年生活費現金」 | 「常見做法之一，是另外保留一段時間可動用的低風險資產……要保留多久取決於你的支出、其他收入與資產配置」（不換成另一個死數字） |

### 清查方式
`scratch/claims.mjs` 把說明頁全部分頁展開、加上 `ANIMAL_META` 與整個 body，
**先剝掉 HTML 註解與 JS 註解**（不剝的話會把我自己記錄「原文寫過什麼」的註解當成殘留），
再掃 13 個關鍵詞。目前殘留 **0**。

> 過程中我重複犯了同一個毛病三次：把「原文寫成 X，已更正」印在**使用者看得到的地方**。
> 使用者要的是現在正確的說法，不是這一頁的版本沿革。三處都移進程式註解了。

---

## 六、目前狀態（重要）

容器在 2026-08-20 07:50 第 6 次回捲。

**倖存**：`index.html`（產品完好）。`source.html` 已由它反推重建，rebuild 驗到 byte-identical。

**遺失**：這幾輪對 69 支回歸測試的修正（`r13/pebasis.mjs`、`r13/animalmeta.mjs`、
`r13/fsdefault.mjs` 三支新測試，以及 `fold`／`par`／`fcf`／`misc85`／`sec88`／`audit`／
`t100db`／`rtclock`／`overlap`／`lookup`／`warn`／`broken` 的更新）、`tests.manifest`、
`test-baseline.json`、以及本機的 `.git`。

也就是說：**產品每次都活下來，工程資產每次都死。**
反推得回產品，反推不回測試。

在 repo 推上你自己的 GitHub 之前，這件事會繼續發生。
